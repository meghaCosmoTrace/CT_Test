package com.automationpractice.elements;

import org.openqa.selenium.By;

public class TextBox extends BaseElements {

    public TextBox(By locator) {
        super(locator);
    }

    // Additional methods specific to text box elements, if needed
}
